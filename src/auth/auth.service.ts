import { Injectable } from '@nestjs/common';

@Injectable()
export class AuthService {
	// public async findUser(
	// 	id: string,
	// ) {
	// 	const existingUser = await this.mongoUserService.getUser(
	// 		id,
	// 		'id',
	// 	);
	// 	if (!existingUser) return null;
	// 	return existingUser;
	// }
}
